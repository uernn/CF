name: Auto Fetch & Deduplicate IPv4 List

on:
  schedule:
    # 每天 UTC 时间 0 点自动执行一次（北京时间早上 8 点）
    - cron: '0 0 * * *'
  workflow_dispatch: 

jobs:
  update-file:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch, Clean and Deduplicate IP list
        # 1.下载 -> 2.删回车符 -> 3.删首尾行并去除Mia优选 -> 4.删尾部IP -> 5.【关键】删所有空格和| -> 6.去重 -> 保存
        run: |
          curl -sL https://bestcf.pages.dev/xinyitang3/ipv4.txt | tr -d '\r' | sed -e '1d;$d' -e 's/[Mm]ia优选//g' -e 's/ | [^ |]*$//' -e 's/[ |]//g' | awk 'NF && !seen[$0]++' > ipv4.txt

      - name: Commit and push if changed
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add ipv4.txt
          
          if git diff --staged --quiet; then
            echo "文件内容无实质变化，无需提交"
          else
            git commit -m "Auto-update: clean spaces and pipes"
            git push
          fi
